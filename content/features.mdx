import FeaturesList from '../components/FeaturesList'
import { FeatureSummary } from '../components/Counts'

# Feature Explorer

<FeatureSummary /> — filter, search, and explore the complete capability matrix.

<FeaturesList/>
